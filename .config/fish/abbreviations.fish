abbr -a g git
abbr -a d docker
# From isabelroses/dotfiles — non-conflicting additions
abbr -a df   'df -h'
abbr -a fd   'fd --hidden'
abbr -a jctl 'journalctl -p 3 -xb'
abbr -a mkd  'mkdir -pv'
abbr -a rs   'systemctl reboot'
